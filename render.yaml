services:
  - type: web
    name: goal-scanner-full
    env: docker
    plan: free
    autoDeploy: true
    disk:
      name: data
      mountPath: /data
      sizeGB: 1
    healthCheckPath: /api/health

