services:
  - name: goal-scanner
    type: web
    runtime: docker
    repo: https://github.com/SEU_USER/SEU_REPO
    plan: free
    envVars:
      - key: APIFOOTBALL_KEY
        sync: false
      - key: ENVIRONMENT
        value: "production"
    healthCheckPath: /api/health
    autoDeploy: true
